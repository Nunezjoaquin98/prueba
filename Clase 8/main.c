#include <stdio.h>
#include <stdlib.h>
#include <string.h>
#define A 3

int main()
{
    /*  sAlumno miAlumno = {4545,"Gigliotti",3} ;
      printf("%d     %s    %0.2f",miAlumno.legajo,miAlumno.nombre,miAlumno.promedio) ;
      return 0;

      ------------ Asi para mostrar yo el dato que introduzco -------------
      */

/*
    sAlumno miAlumno ;

    printf("Ingrese legajo: ") ;
    fflush(stdin) ;
    scanf("%d", &miAlumno.legajo) ;

    printf("Ingrese el nombre alumno: ") ;
    fflush(stdin) ;
    gets(miAlumno.nombre) ;

    printf("Ingrese edad") ;
    fflush(stdin) ;
    scanf("%d",&miAlumno.edad) ;

    printf("Ingrese promedio: ") ;
    fflush(stdin) ;
    scanf("%f", &miAlumno.promedio) ;
    system("cls") ;

    printf("Legajo     Nombre     Edad     Promedio\n") ;
    mostrarUnalumno(miAlumno) ;

    asi muestra con funcion mi alumno
*/
/*
sAlumno listadoMain[A] ;
for(int i=0; i<A; i++)
{
    printf("Ingrese legajo: ");
    scanf("%d",&listadoMain[i].legajo) ;
    printf("Ingrese el nombre alumno: ") ;
     fflush(stdin) ;
     gets(listadoMain[i].nombre) ;

    printf("Ingrese edad") ;
    fflush(stdin) ;
    scanf("%d",&listadoMain[i].edad) ;

     printf("Ingrese promedio: ") ;
     fflush(stdin) ;
     scanf("%f", &listadoMain[i].promedio) ;
}
printf("Legajo     Nombre     Edad     Promedio\n") ;
for(int i=0; i<A; i++)
{
    mostrarUnalumno(listadoMain[i]);

    acceder a elemento del  arrays para ir mostrando varios alumnos

    */



}
